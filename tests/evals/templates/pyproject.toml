[project]
name = "api-client-fixture"
version = "0.1.0"
description = "A simple API client for GSD-Lite evaluation"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"